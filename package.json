{
  "name": "klarastad-form",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:unit": "vitest run src tests/unit tests/fixtures",
    "test:integration": "vitest run tests/integration",
    "test:integration:local": "supabase start && supabase db reset && set -a && eval \"$(supabase status -o env | sed -n 's/^ANON_KEY=/SUPABASE_ANON_KEY=/p; s/^API_URL=/SUPABASE_URL=/p; s/^SERVICE_ROLE_KEY=/SUPABASE_SERVICE_ROLE_KEY=/p; s/^FUNCTIONS_URL=/SUPABASE_FUNCTIONS_URL=/p')\" && set +a && DISABLE_OFFER_RATE_LIMIT=1 RUN_DB_TESTS=1 npm run test:integration",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@vitejs/plugin-react": "^4.3.1",
    "jsdom": "^29.1.1",
    "vite": "^5.4.10",
    "vitest": "^4.1.5"
  }
}
